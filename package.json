{
  "name": "learning-webpack",
  "version": "0.1.0",
  "description": "as in title",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-build": "./node_modules/.bin/webpack",
    "start-webpack": "start bash -c \"./node_modules/.bin/webpack --watch\"",
    "start-server": "start bash -c \"./node_modules/.bin/live-server --entry-file=index.html --host=localhost\"",
    "start": "npm run-script start-webpack && npm run-script start-server"
  },
  "author": "Maciej Mazur",
  "license": "none",
  "devDependencies": {
    "babel-core": "^6.7.2",
    "babel-loader": "^6.2.4",
    "babel-preset-es2015": "^6.6.0",
    "webpack": "^1.12.14"
  },
  "dependencies": {
    "jquery": "^2.2.1"
  }
}
